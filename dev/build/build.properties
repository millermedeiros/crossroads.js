dev.dir = dev
src.dir = ${dev.dir}/src
build.dir = ${dev.dir}/build
dist.dir = dist

closure_compiler.jar = ${build.dir}/closure_compiler/compiler.jar
rhino.jar = ${build.dir}/rhino/js.jar
jshint.js = ${build.dir}/jshint/jshint-rhino.js

product.name = crossroads
version.number = 0.6.0+

dist.name = ${product.name}.js
dist.min.name = ${product.name}.min.js

jshint.opts = boss=true,expr=true,eqeqeq=true,eqnull=true,immed=true,newcap=true,noarg=true,noempty=true,nonew=true,onevar=true
